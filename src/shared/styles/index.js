import {StyleSheet} from 'react-native'

const globalStyles = StyleSheet.create({
    colorPrimary:{
        color: "#fff"
    },
    inspect: {
        borderColor: 'green',
        borderWidth: 2
    },
})

export default globalStyles